import { z } from "zod";

export const SymbolRequest = z.object({ "locate": z.union([z.object({ "file_path": z.string(), "line": z.union([z.number().int(), z.array(z.any()).min(2).max(2)]), "find": z.string(), "position": z.enum(["start","end"]).default("start") }), z.object({ "file_path": z.string(), "symbol_path": z.array(z.string()) }).describe("Locate by symbol path")]), "include_hover": z.boolean().default(true), "include_content": z.boolean().default(true) }).describe("Retrieves detailed information about a symbol at a specific location.\n\nUse this to get the documentation (hover) and source code implementation\nof a symbol to understand its purpose and usage.");
