import { z } from "zod";

export const ImplementationRequest = z.object({ "locate": z.union([z.object({ "file_path": z.string(), "line": z.union([z.number().int(), z.array(z.any()).min(2).max(2)]), "find": z.string(), "position": z.enum(["start","end"]).default("start") }), z.object({ "file_path": z.string(), "symbol_path": z.array(z.string()) }).describe("Locate by symbol path")]), "include_hover": z.boolean().default(false), "include_content": z.boolean().default(true), "limit": z.union([z.number().int(), z.null()]).default(null), "offset": z.number().int().default(0) }).describe("Finds concrete implementations of an abstract symbol or interface.\n\nUse this when you are at an interface or base class method and want to see\nhow it is actually implemented in subclasses.");
