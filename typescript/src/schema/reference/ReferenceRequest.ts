import { z } from "zod";

export const ReferenceRequest = z.object({ "locate": z.union([z.object({ "file_path": z.string(), "line": z.union([z.number().int(), z.array(z.any()).min(2).max(2)]), "find": z.string(), "position": z.enum(["start","end"]).default("start") }), z.object({ "file_path": z.string(), "symbol_path": z.array(z.string()) }).describe("Locate by symbol path")]), "include_hover": z.boolean().default(false), "include_content": z.boolean().default(true), "limit": z.union([z.number().int(), z.null()]).default(null), "offset": z.number().int().default(0) }).describe("Finds all references (usages) of a symbol.\n\nUse this to see where a function, class, or variable is used across the codebase,\nwhich is essential for refactoring or understanding the impact of a change.");
