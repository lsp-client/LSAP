import { z } from "zod";

export const InlineValueRequest = z.object({ "file_path": z.string(), "range": z.object({ "start": z.object({ "line": z.number().int().gte(1), "character": z.number().int().gte(1) }).describe("Represents a specific position in a file using line and character numbers.\n\nNote: Line and character are 1-based indices. 0-based indices are used in LSP, so conversion is needed when interfacing with LSP."), "end": z.object({ "line": z.number().int().gte(1), "character": z.number().int().gte(1) }).describe("Represents a specific position in a file using line and character numbers.\n\nNote: Line and character are 1-based indices. 0-based indices are used in LSP, so conversion is needed when interfacing with LSP.") }) }).describe("Retrieves runtime or contextual values for variables in a range.\n\nUse this when debugging or inspecting code to see the actual values\nof variables at specific lines.");
