import { z } from "zod";

export const InlayHintRequest = z.object({ "file_path": z.string(), "range": z.union([z.object({ "start": z.object({ "line": z.number().int().gte(1), "character": z.number().int().gte(1) }).describe("Represents a specific position in a file using line and character numbers.\n\nNote: Line and character are 1-based indices. 0-based indices are used in LSP, so conversion is needed when interfacing with LSP."), "end": z.object({ "line": z.number().int().gte(1), "character": z.number().int().gte(1) }).describe("Represents a specific position in a file using line and character numbers.\n\nNote: Line and character are 1-based indices. 0-based indices are used in LSP, so conversion is needed when interfacing with LSP.") }), z.null()]).default(null) }).describe("Retrieves inline hints like parameter names or inferred types.\n\nUse this to get better context for code by seeing \"hidden\" details\nthat are normally provided by an IDE's visual overlay.");
